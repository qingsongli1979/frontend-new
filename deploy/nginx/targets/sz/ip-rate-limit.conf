# Proxy extraction and package APIs are authenticated and quota-controlled by the
# IP service. Do not throttle them by source IP here: multiple developers and
# workers commonly share one company NAT address.
