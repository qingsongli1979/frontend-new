# 123Proxy 开发者中心方案

版本：V1.0  
目标用户：爬虫开发工程师、数据平台工程师、AI 数据采集团队

## 1. 核心目标

开发者中心不是官网产品页的延伸，也不是客服问题汇总。它应帮助工程师完成四件事：

1. 在 5 分钟内发出第一个成功的代理请求。
2. 准确理解不同产品的认证、地区、SESSION、轮转和提取逻辑。
3. 将代理接入现有代码、爬虫框架和分布式采集任务。
4. 请求失败时，能够通过错误码、测试命令和检查清单自行定位问题。

首屏核心承诺建议使用：

> 从一条 cURL 开始，接入 123Proxy 全球代理网络

辅助文案：

> 选择代理产品，配置认证、地区与 SESSION，复制可运行的代码示例。

首要 CTA：

- 5 分钟快速开始
- 按产品查看接入方式
- 登录控制台生成代理

## 2. 现状判断

### 2.1 当前官网

- “开发者”菜单主要指向首页锚点和旧文档链接，没有形成完整任务路径。
- “快速接入”“代码示例”“帮助与排查”之间内容重复，入口与结果不明确。
- 页脚中的 API 文档、错误码和状态页仍有空链接。

### 2.2 当前旧文档

- 文档站标题仍显示 `My Site`，与新版品牌不一致。
- 内容按旧套餐名称组织，与当前五类标准代理产品无法一一对应。
- 部分内容已经错误或过时，例如静态代理白名单、地区数量、动态产品分类。
- “按 IP 池套餐”分类已经无法正常进入文档。
- 代理集成主要是 AdsPower、BitBrowser 和 Chrome，不足以服务爬虫工程师。
- 大量步骤依赖旧控制台截图，无法作为稳定的技术契约。

### 2.3 当前控制台

新版控制台已经包含较完整的真实产品规则：

- HTTP(S) / SOCKS5
- 账密认证与套餐级 IP 白名单
- HOSTNAME / IP 接入
- 四种代理输出格式
- cURL、Python、Node.js、Go、Java、PHP 示例
- 国家定位、SESSION、轮转周期和静态 IP 分配

问题在于这些知识仍嵌在交互代码中，没有沉淀为可搜索、可引用、可维护的公开文档。

## 3. 竞品可借鉴之处

快代理帮助中心值得借鉴的不是页面视觉本身，而是任务拆分方式：

- 首页先提供搜索，再提供快速入门、产品介绍、开发者指南、API、SDK 等明确入口。
- 每个产品有独立开发手册，并包含总览、认证、产品参数、测试、频率限制、错误码和开发建议。
- “测试代理可用性”独立成文，直接提供 HTTP、SOCKS 和 cURL 验证方法。
- API 文档把授权、返回格式、公共错误码和各产品接口分开。
- 文档页面有左侧导航、页内目录、上一篇/下一篇和内容反馈。

123Proxy 不应照搬的部分：

- 不把购买、实名认证、浏览器教程和开发 API 混在同一层级。
- 不为标准代理协议包装没有必要的 SDK。
- 不公开尚未版本化、没有稳定授权契约的控制台内部 API。
- 不用大量重复文章分别解释相同的认证和代码逻辑。

## 4. 推荐 URL 架构

开发文档保留在主站域名下，控制台继续使用独立域名。

```text
https://www.123proxy.cn/developers/
https://www.123proxy.cn/developers/getting-started/
https://www.123proxy.cn/developers/products/...
https://www.123proxy.cn/developers/guides/...
https://www.123proxy.cn/developers/code-examples/...
https://www.123proxy.cn/developers/api/...
https://www.123proxy.cn/developers/troubleshooting/...
```

原因：

- 开发文档可继承主站 SEO 权重和企业信任。
- 产品页与技术文档之间跳转更自然。
- 静态渲染、站内搜索、canonical 和 sitemap 更容易统一。
- 将来英文版可平行放在 `/en/developers/`，当前中文页头不需要展示英文入口。

旧的 `/document/docs/...` 应建立逐页 301 映射，不应统一重定向到文档首页。

## 5. 顶部“开发者”菜单

菜单不直接展开几十篇文章，只提供六个稳定入口：

| 入口 | 说明 |
| --- | --- |
| 快速开始 | 从购买或测试到第一个成功请求 |
| 产品接入 | 六类代理产品的真实接入方式 |
| 认证与路由 | 账密、白名单、地区、SESSION、轮转 |
| 代码示例 | cURL、Python、Node.js、Go、Java、PHP |
| API 参考 | 提取链接、参数、返回格式与限制 |
| 错误码与排查 | 可用性测试、错误码和生产检查清单 |

菜单左侧品牌文案：

> 开发者中心  
> 从一条 cURL 到生产数据采集

## 6. 开发者中心信息架构

### 6.1 开始使用

- 开发者中心首页
- 5 分钟快速开始
- 如何选择代理产品
- 如何在控制台生成代理
- 使用 cURL 验证 HTTP(S) / SOCKS5

### 6.2 产品接入

- 高带宽代理 IP
- 隧道代理
- 隧道住宅代理
- 不限量动态住宅
- 长效静态代理
- 长效静态住宅

### 6.3 核心概念

- 代理账号与网站登录账号的区别
- 账密认证与套餐级 IP 白名单
- HOSTNAME 与 IP 接入地址
- HTTP(S) 与 SOCKS5
- 全球随机、粗粒度地区与国家定位
- 轮转、SESSION 与固定 IP 的区别
- 代理输出格式
- 并发线程、端口、流量与带宽
- 配置同步与生效时间

### 6.4 代码与框架

基础语言：

- cURL
- Python Requests
- Python HTTPX / aiohttp
- Node.js Fetch / Axios
- Go `net/http`
- Java `HttpClient`
- PHP cURL

爬虫与自动化框架：

- Scrapy
- Playwright
- Selenium
- 浏览器自动化的并发计算
- 分布式 worker 的 SESSION 和端口分配

### 6.5 API 参考

首期仅文档化已稳定支持的代理提取能力：

- 动态代理提取链接
- 静态 IP 分配链接
- 请求参数
- 文本输出格式
- 返回示例
- 调用限制
- 错误返回

账号、订单、支付和内部管理接口不应在首期公开。只有在完成 API 版本号、Token 权限、限流、错误模型和兼容策略后，才建设 Management API。

### 6.6 运行与排查

- 代理可用性测试
- 检查出口 IP 和国家
- 407、超时、连接失败和目标站点错误
- 并发、连接池、Keep-Alive、超时与重试
- 流量、带宽和请求成功率
- 凭证安全
- 服务状态
- 更新日志

## 7. 产品文档的真实能力边界

### 7.1 高带宽代理 IP

- 项目制交付，不是标准套餐。
- 项目内不限流量，10Gbps+ 单项目聚合容量。
- HTTP(S) / SOCKS 标准接入。
- 支持账密或 IP 白名单，最终以项目配置为准。
- 代理池、地区、出口和会话策略按项目设计。
- 文档重点应是 worker 接入、连接池、并发、重试、吞吐测量和生产排查。
- 不提供虚构的公共提取 API。

### 7.2 隧道代理

- 爬虫混合池：约 95% 住宅 IP + 约 5% 数据中心 IP，通常采集更快。
- 纯住宅池：只包含住宅 IP，并支持 SESSION。
- 默认全球随机；提取时可选欧美、北美、欧洲、亚洲、美国、日韩等粗粒度地区。
- 支持按流量或按并发线程购买；并发线程套餐不限流量。
- 支持 HOSTNAME / IP、HTTP(S) / SOCKS5。
- 支持账密或套餐级 IP 白名单。
- SESSION 仅适用于纯住宅池的账密认证。
- SESSION ID 为 12 位字母或数字，保持时长为 1-120 分钟。

### 7.3 隧道住宅代理

- 8000 万+住宅 IP，覆盖 190+国家和地区。
- 仅按流量购买。
- 默认全球随机，国家或地区在提取时选择。
- 国家码以小写后缀放在认证用户名最后，例如 `+us`。
- SESSION 位于代理用户名与国家码之间。
- 完整结构：

```text
<代理用户名>-sess_<12位SESSION_ID>_<分钟>+<国家码>
```

- 支持账密或套餐级 IP 白名单。
- 首次生成或修改国家、SESSION 后，通常需要约 3-15 分钟同步。

### 7.4 不限量动态住宅

- 按端口购买，每个端口不限流量、每个端口不限并发。
- 套餐包含一段连续端口，程序应使用实际端口范围，而不是把提取 API 返回的单个网关端口当作套餐端口。
- 每个端口在 3-30 分钟周期内保持一个住宅出口，周期结束后自动轮转。
- 不需要在代理用户名中手工设置 SESSION。
- 国家或地区与轮转周期按套餐统一设置，不能为单个端口分别配置。
- 支持账密或套餐级 IP 白名单。
- 代码示例应说明如何将不同 worker 分配到不同端口。

### 7.5 长效静态代理

- 独享固定数据中心 IP，不限流量。
- 选择国家、协议和数量后生成“静态 IP 分配链接”。
- 调用分配链接后才会占用套餐剩余数量并返回固定代理。
- 分配链接不是代理地址，不能直接放进 Requests、Scrapy 或浏览器。
- 只有分配后返回的 `HOST / PORT / USER / PASSWORD` 才能用于程序。
- 不支持 IP 白名单、SESSION 或轮转。
- 不支持中国地区 IP。

### 7.6 长效静态住宅

- 独享固定住宅 ISP IP，不限流量。
- 与长效静态代理一样，先调用分配链接，再使用返回的固定代理信息。
- 不支持 IP 白名单、SESSION 或轮转。
- 国家范围按实时住宅 ISP 库存决定。
- 文档应解释续费、资源健康、固定身份和故障替换边界。

## 8. 代码示例标准

每个示例必须满足：

- 可以直接运行，不是伪代码。
- 使用环境变量保存代理用户名和密码。
- 同时展示 HTTP(S) 和 SOCKS5 的差异。
- 设置连接超时、读取超时和失败处理。
- 不把真实用户凭证写入文档。
- 清楚区分“调用提取 API”和“通过代理访问目标 URL”。
- 静态代理必须先展示分配步骤，再展示返回代理的使用代码。
- 不限量动态住宅示例必须说明代码只展示端口范围中的一个端口。
- 隧道住宅示例必须展示认证用户名的分段结构和国家码顺序。

建议首期语言：

```text
cURL / Python / Node.js / Go / Java / PHP
```

建议首期框架：

```text
Scrapy / Playwright / Selenium
```

SDK 暂不作为首期重点。123Proxy 使用标准代理协议，可靠、可复制的框架示例比包装 SDK 更有价值。

## 9. 单篇产品文档模板

每个产品文档保持相同阅读顺序：

1. 这个产品如何连接
2. 3 分钟最小可运行示例
3. 在控制台如何生成或分配代理
4. 接入地址、认证和协议
5. 地区、SESSION、轮转或固定 IP 逻辑
6. 认证用户名结构拆解
7. 提取 API 参数与输出格式
8. 六种语言代码示例
9. 预期结果与出口验证
10. 限制、错误码与排查
11. 生产环境建议

页面顶部必须立即展示：

- 产品类型
- 认证方式
- 地区能力
- 轮转方式
- 计费维度
- “打开控制台生成代理”按钮

## 10. 页面视觉与交互

开发者文档应比营销页更克制、密度更高：

- 顶部：品牌、文档搜索、服务状态、登录控制台。
- 左侧：固定文档树，宽度约 248-264px。
- 中间：正文最大宽度约 800-880px。
- 右侧：当前文章目录，宽度约 200-224px。
- 代码区使用深色背景，正文保持白色背景。
- 参数表、警告、产品边界和返回示例使用不同语义色，不堆叠装饰卡片。
- 桌面端代码与说明可并排，移动端按说明、代码、结果顺序纵向排列。
- 页面提供复制代码、复制代理格式、切换语言、锚点链接和文章反馈。

文档首页不做营销式大 Hero。首屏应直接包含搜索、快速开始和产品入口。

## 11. 关键交互

### 11.1 文档搜索

搜索结果按以下优先级排序：

1. 产品名和常用别名
2. 错误码
3. 参数名
4. 语言与框架
5. FAQ

需要建立同义词：

```text
隧道代理 / Scraping rotating proxy
隧道住宅 / Residential rotating proxy
粘性会话 / Sticky session / SESSION
白名单 / allowlist / whitelist
主机 / hostname / gateway
```

### 11.2 无凭证代码生成器

文档可以提供只生成示例的配置器：

- 产品
- HTTP(S) / SOCKS5
- 账密 / 白名单
- 国家或地区
- SESSION
- 输出格式
- 代码语言

生成器只使用占位符，不读取或保存真实凭证。真实套餐和代理信息仍由控制台生成。

### 11.3 控制台深链接

产品文档的 CTA 应进入对应控制台产品，而不是泛化登录页：

```text
https://console.123proxy.cn/app/#product-tunnel
https://console.123proxy.cn/app/#product-residential
https://console.123proxy.cn/app/#product-unlimited
https://console.123proxy.cn/app/#product-static-datacenter
https://console.123proxy.cn/app/#product-static-residential
```

登录后应保留目标 hash，直接回到对应产品。

## 12. 单一事实来源

为避免官网、控制台和开发文档再次出现信息不一致，应建立公开产品能力配置：

```text
产品名称
产品 key
计费方式
认证方式
协议
接入地址类型
地区能力
SESSION / 轮转规则
输出格式
国家列表
代码语言
控制台路由
```

产品页、价格页、控制台提示和开发文档共同读取这份配置。后台内部参数、接口密钥和不可公开字段不进入公开配置。

构建检查应验证：

- 静态产品不出现白名单和 SESSION。
- 隧道住宅只出现按流量计费。
- 不限量动态住宅始终显示套餐级地区和 3-30 分钟轮转。
- 隧道代理默认全球随机，SESSION 只属于纯住宅池账密认证。
- 所有控制台深链接指向新版控制台。

## 13. SEO、GEO 与静态渲染

- 所有文档静态渲染，代码和参数必须存在于 HTML 中。
- 每篇文章提供独立 title、description、canonical 和面包屑。
- 使用 `TechArticle`、`HowTo`、`FAQPage` 和 `BreadcrumbList` 结构化数据。
- 文档页进入 sitemap，并提供准确的 `lastmod`。
- 页面标题使用用户问题，例如“如何为隧道住宅代理设置国家和 SESSION”。
- 关键回答先给结论，再给步骤和代码，方便搜索与 AI 摘要引用。
- 未来英文文档使用 `/en/developers/`，不要把英文参数名翻译成不兼容的中文名称。

## 14. 实施顺序

### 第一批：开发者能够完成真实接入

- 开发者中心首页
- 5 分钟快速开始
- 六个产品接入页
- 认证、地区、SESSION、输出格式四篇核心概念
- cURL、Python、Node.js、Go、Java、PHP 示例
- Scrapy、Playwright 示例
- 代理可用性测试
- 基础错误码与排查
- 官网菜单、页脚和产品页入口更新
- 旧文档逐页 301

### 第二批：形成完整工程知识库

- 提取 API 参考
- HTTPX、aiohttp、Axios、Selenium
- 分布式采集、连接池、重试和并发建议
- 服务状态、更新日志和文章反馈
- 文档搜索与同义词

### 第三批：API 产品化后再建设

- Management API
- OpenAPI 3.1 规范
- Token 权限与轮换
- API 版本与兼容策略
- 官方 SDK

## 15. 验收标准

- 新用户从文档首页到成功 cURL 请求不超过 5 分钟。
- 每种产品都能找到与控制台一致的生成、提取或分配流程。
- 任何代码示例都不会把静态分配链接误当作代理地址。
- 任一产品的认证、地区、SESSION 和白名单边界没有冲突。
- 桌面和手机端均可访问完整左侧目录、搜索、代码复制和控制台入口。
- 旧文档 URL 不产生 404，且重定向到对应新文章。
- 文档、官网产品页与控制台的核心产品事实通过自动检查保持一致。

## 16. 参考

- 快代理帮助中心：https://www.kuaidaili.com/helpcenter/
- 快代理隧道代理开发手册：https://www.kuaidaili.com/doc/dev/tps/
- 快代理海外动态代理开发手册：https://www.kuaidaili.com/doc/dev/fps/
- 快代理代理可用性测试：https://www.kuaidaili.com/doc/dev/proxy/
- 快代理 API 接口概览：https://www.kuaidaili.com/doc/api/
