# 123Proxy 首页核心文案 V3

## 品牌定位

面向爬虫工程师和 AI 数据团队的企业级全球代理，其中 AI 数据方案重点推荐高带宽代理 IP。

## 首屏

### Eyebrow

High-bandwidth Proxy for AI

### H1

不限流量海外代理 IP，为数据采集提速

### 副标题

为爬虫工程师稳定调度全球代理，为 AI 数据团队提供高带宽、不限流量的代理 IP，持续采集公开文本、图片、视频与代码数据。

### CTA

- 免费测试 1GB
- 查看开发文档
- 高带宽代理 IP

### 工程能力

- 高带宽 / 不限流量
- HTTP(S) / SOCKS / API
- 定制代理池
- 7x24 技术支持

### 关键指标

- 8000万+ 全球代理 IP 资源池
- 190+ 国家和地区覆盖
- 99.99% 服务可用性目标
- 7x24 企业技术支持

## 核心代理产品

### 标题

核心代理产品，从 AI 大规模采集到常规爬虫

### 说明

AI 视频、图片与代码下载优先选择高带宽代理；常规网页抓取再按住宅属性、轮换方式和固定会话选择产品。

### 高带宽代理 IP

面向视频、图片、音频、代码与全网文本等大规模下载任务，提供不限流量、项目级带宽和按目标站点定制的代理池。

### 隧道代理

提供爬虫混合池与纯住宅 IP 池。混合池由约 95% 住宅 IP 和约 5% 数据中心 IP 组成，采集速度通常比隧道住宅代理更快；纯住宅池只包含住宅 IP，并支持 SESSION。两个池均为全球随机出口，支持按流量或不限流量的并发线程方案。

### 隧道住宅代理

通过固定隧道接入 8000万+ 住宅代理 IP，覆盖 190+ 国家和地区。支持在鉴权中指定国家或地区与 SESSION，仅按实际传输流量购买，不提供并发线程套餐。

### 不限量动态住宅代理

每个端口不限流量且不限并发，出口 IP 按套餐设定的固定周期每 3–30 分钟轮转。地区只支持套餐级设置，不能为每个端口单独指定。

### 并发线程说明

按并发线程使用的隧道产品更适合代码直接通过 HTTP 协议请求。一个同时在途的请求占用一个线程；浏览器打开一个页面还会并行加载图片、CSS、JS、字体等资源，通常需要约 10–20 个并发线程。

### 长效静态代理

基于高性能云主机的独享固定机房 IP，适合固定出口、账号环境隔离、自动化浏览器与长时间会话。

### 长效静态住宅代理

固定住宅网络出口兼顾住宅属性与长时间会话，适合需要地区一致性、连续登录态和稳定身份环境的任务。

## AI 数据采集

### 标题

AI 大规模采集，先解决带宽瓶颈

### 说明

视频、图片、音频和代码仓库进入规模化下载后，按 GB 计费会让成本随数据量持续增长。高带宽代理 IP 用不限流量、定制代理池和项目级吞吐支撑长期任务。

### 数据管道

公开数据源 → 123Proxy 高带宽代理 → 客户采集集群 → 客户数据存储

### 场景

- 视频与多模态数据下载
- 大规模图片数据集采集
- 全网文本语料持续采集
- 公开代码与仓库数据采集

每个场景展示推荐带宽、推荐产品和不限流量的成本优势，主 CTA 统一指向高带宽代理产品。

## 开发者

### 标题

工程师可以先跑通，再做采购

### 说明

首页直接给出真实接入方式、主流框架样例和请求状态。减少售前沟通，让 PoC 验证尽可能接近生产环境。

### 能力

- Python、cURL、Scrapy、Playwright、Node.js 样例
- HTTP(S)、SOCKS 与 API 提取方式
- 代理可用性测试、错误码与状态页
- 流量、带宽、请求状态与任务排障

## 全球网络

### 标题

面向全球任务的代理网络能力

### 说明

通过大规模资源池、区域选择、自动轮换和固定会话，支撑跨区域公开数据访问与持续任务运行。

## 企业服务

### 标题

从代理采购升级为可持续的数据基础设施

### 说明

企业客户不仅需要可用 IP，也需要资源保障、任务可观测、技术支持和清晰的合规使用边界。

### 能力

- 企业 SLA 方案
- 专属资源池
- 任务可观测
- 7x24 技术支持
- 合规使用边界
- 采集架构咨询

## 采购路径

### 标题

从工程验证到企业级规模化采集

### 免费测试 1GB

验证目标区域、代理协议、认证方式与采集代码。

### 标准代理产品

按流量、端口或 IP 选择产品，适配持续网页采集和账号会话任务。

### AI 数据采集方案

面向视频、图片、文本、代码等大规模训练数据任务。

## 最终 CTA

### 标题

先让采集任务跑起来，再把它扩展到生产规模

### 说明

使用 1GB 测试流量验证目标站点、地区与代码接入。AI 数据集项目可直接评估带宽、代理策略与任务规模。

### CTA

- 免费测试 1GB
- 咨询采集架构师
